Check the API documentation opening the docs endpoint: 

http://127.0.0.1:8000/docs.

Run the project with the command line:

uvicorn main:app --reload


Docker: 

docker compose up --build

En segundo plano:
docker compose up -d --build

